#ifndef PIPEX_H
# define PIPEX_H

# include <unistd.h>
# include <stdlib.h>
# include <fcntl.h>

// Step 1: Include necessary function prototypes
// For example:
// - find_command_path() to search for the full path of a command
// - Any utility functions for string manipulation (ft_split, ft_strjoin, etc.)

// Step 2: Include necessary system libraries for I/O operations (e.g., fcntl.h for open(), unistd.h for pipe(), execve(), etc.)

#endif